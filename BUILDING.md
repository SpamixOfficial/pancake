# Building pancake

First, install all dependencies

## Dependencies
- Rust (2024 edition support needed)
- Cargo
- Bun v1.3.x

## Building

To build the project simply run `cargo build --release` in the project root. This will build both the web UI and the project

## Development mode